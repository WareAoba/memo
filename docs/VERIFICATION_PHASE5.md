# Phase 5 검증 결과

검증일: 2026-09-24

## 구현

- schedules / schedule_entity_snapshot / schedule_tasks / schedule_task_items migration.
- 일정 생성·목록·상세·부분 수정·보관·복원. 날짜/기간/워크 필터, 페이지 제한, 실제 날짜·시간·IANA 시간대 검증.
- 활성 워크·태스크 소유권 검사. 선택 순서 보존, 중복 제거, 최대 100개.
- 단일 BEGIN IMMEDIATE transaction으로 워크·태스크·항목 정의와 기본 실행 값을 복사. 원본 수정·보관과 독립적인 이력.
- 일정 생성 화면: 워크 검색, 활성 기본 태스크 초기 선택, 태스크 검색·추가·제거·재정렬, 오류와 재시도.
- 일정 관리 화면: 목록·날짜 필터·보관함·생성·수정·보관·복원. 저장 실패 시 입력 유지.
- 기존 3개 주 탐색 유지. Today/캘린더 상단에서 일정 관리로 이동.

## 자동 검사

- npm run check:backend 통과: rustfmt, clippy(-D warnings), 23 tests, build.
- npm run check:frontend 통과: Prettier, ESLint, 58 tests, TypeScript/Vite build.
- 새 백엔드 회귀 4개: 프리셋/워크 편집·보관 후 스냅샷 유지 및 새 일정의 최신 정의, 기본값/버전/중복, 재접속 영속성, 수정·보관·복원, 날짜/시간/필터 오류, 소유권/복합 FK, 항목 삽입 실패 전체 rollback.
- 새 프런트 회귀 5개: 활성 기본 선택·순서, 저장 실패 후 입력 보존·재저장, 기본 조회 실패와 재시도, 시간 검증·수정 시 스냅샷 제외, 목록 재시도·보관함, 상세 스냅샷 표시·보관.

## 브라우저 확인

- 개발 백엔드를 실행하고 기존 Vite 서버에서 실제 API 연결 확인.
- 기존 Phase 4 검증용 워크 선택 → 연결된 기본 태스크 표시 → Phase 5 검증용 일정 생성.
- 상세 워크·태스크 스냅샷, 새로고침 후 유지, 일정 메모 수정·저장 확인.
- 데스크톱 상세 화면 및 390×844 모바일 편집 화면의 레이아웃 확인. 임시 뷰포트 설정은 복구했다.
- 검증용 일정 ID: 4ade30f9-5f49-4200-a1dc-32f9f13cc32a. 로컬 DB에 남겨 두었다.

## 범위와 다음 단계

- 워크·태스크 정의는 생성 시 확정한다. 일정 수정은 날짜·시간·시간대·제목·메모에 한정한다. 다른 구성이 필요하면 새 일정을 만든다.
- 동시 메타데이터 PATCH는 직렬화하며 충돌 감지 UI는 제공하지 않는다.
- Today/월 캘린더는 여전히 명시적인 예시 화면이다. 실제 일정은 별도 일정 관리에서 조회한다.
- 다음은 Phase 6 Today·진행률·요건·일정 상세 연결이다. 실행 체크는 Phase 7, 월 캘린더 실데이터는 Phase 8이다.
- Docker 재빌드 검증은 이번 단계에서 수행하지 않았다.
