{
  "name": "preset-execution",
  "private": true,
  "scripts": {
    "dev:backend": "cargo run --manifest-path backend/Cargo.toml",
    "dev:frontend": "npm --prefix frontend run dev",
    "format:backend": "cargo fmt --manifest-path backend/Cargo.toml",
    "format:frontend": "npm --prefix frontend run format",
    "check:backend": "cargo fmt --manifest-path backend/Cargo.toml --check && cargo clippy --manifest-path backend/Cargo.toml --all-targets --locked -- -D warnings && cargo test --manifest-path backend/Cargo.toml --locked && cargo build --manifest-path backend/Cargo.toml --locked",
    "check:frontend": "npm --prefix frontend run format:check && npm --prefix frontend run lint && npm --prefix frontend test && npm --prefix frontend run build",
    "check": "npm run check:docs && npm run check:backend && npm run check:frontend",
    "docker:up": "docker compose up --build -d --wait",
    "docker:down": "docker compose down",
    "test:frontend": "npm --prefix frontend test --",
    "test:backend": "cargo test --manifest-path backend/Cargo.toml --locked",
    "check:docs": "node scripts/check-docs.mjs"
  }
}
